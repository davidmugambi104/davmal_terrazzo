import React from 'react';

const CheckInWidget = () => {
    return (
        <div>
            <h2>Check-In Widget</h2>
            <button>Check In</button>
        </div>
    );
};

export default CheckInWidget;
